<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="JPSoftworks.Cocos.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <controls:TitleBar x:Name="MainTitleBar"
                           AutoConfigureCustomTitleBar="True"
                           CanDrag="True"
                           Content="Sticky Windows Companion" />
        <NavigationView
            x:Name="RootNavView"
            Grid.Row="1"
            IsSettingsVisible="True"
            PaneDisplayMode="Left"
            SelectionChanged="OnNavigationSelectionChanged"
            Loaded="OnNavViewLoaded">
            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                <Thickness x:Key="NavigationViewHeaderMargin">15,0,0,0</Thickness>
            </NavigationView.Resources>
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Companions" Icon="Contact" Tag="companions" />
                <NavigationViewItem Content="Models" Icon="Document" Tag="models" />
                <NavigationViewItem Content="Transformations" Icon="Edit" Tag="transformations" />
                <NavigationViewItem Content="Scraps" Icon="Copy" Tag="scraps" />
                <NavigationViewItem Content="Apps" Icon="AllApps" Tag="apps" />
            </NavigationView.MenuItems>
            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Window>
