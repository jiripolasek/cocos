<?xml version="1.0" encoding="UTF-8" ?>
<Application
    x:Class="JPSoftworks.Cocos.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:JPSoftworks.Cocos"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <ResourceDictionary Source="ms-appx:///Styles/TextBox.xaml" />

                <ResourceDictionary>
                    <ResourceDictionary.ThemeDictionaries>
                        <ResourceDictionary x:Key="Dark">
                            <AcrylicBrush x:Key="DarkAcrylicFill" TintColor="#DD000000" TintOpacity="0.7" TintLuminosityOpacity="0.7" />
                            <SolidColorBrush x:Key="DarkAcrylicFillForeground" Color="#FFFFFF" />
                        </ResourceDictionary>
                        <ResourceDictionary x:Key="Light">
                            <AcrylicBrush x:Key="DarkAcrylicFill" TintColor="#DDFFFFFF" TintOpacity="0.7" TintLuminosityOpacity="0.7" />
                            <SolidColorBrush x:Key="DarkAcrylicFillForeground" Color="#000000" />
                        </ResourceDictionary>
                        <ResourceDictionary x:Key="HighContrast">
                            <AcrylicBrush x:Key="DarkAcrylicFill" TintColor="#FF000000" TintOpacity="0.7" TintLuminosityOpacity="0.7" />
                            <SolidColorBrush x:Key="DarkAcrylicFillForeground" Color="#FFFFFF" />
                        </ResourceDictionary>
                    </ResourceDictionary.ThemeDictionaries>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
            <!-- Other app resources here -->
            <Color x:Key="Primary">#512BD4</Color>

            <x:Double x:Key="SettingsCardSpacing">12</x:Double>

            <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource Primary}" />
            <SolidColorBrush x:Key="WhiteBrush" Color="White" />
            <SolidColorBrush x:Key="BlackBrush" Color="Black" />

            <x:Double x:Key="AppFontSize">14</x:Double>

            <Style x:Key="MyLabel" TargetType="TextBlock">
                <Setter Property="Foreground"
                        Value="{StaticResource PrimaryBrush}" />
            </Style>

            <Style x:Key="Action" TargetType="Button">
                <Setter Property="FontSize"
                        Value="{StaticResource AppFontSize}" />
                <Setter Property="Padding"
                        Value="14,10" />
            </Style>

            <Style x:Key="PrimaryAction"
                   TargetType="Button"
                   BasedOn="{StaticResource Action}">
                <Setter Property="Background"
                        Value="{StaticResource PrimaryBrush}" />
                <Setter Property="CornerRadius"
                        Value="8" />
                <Setter Property="Foreground"
                        Value="{StaticResource WhiteBrush}" />
            </Style>

            <Style x:Key="SettingsSectionHeaderTextBlockStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="20" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Margin" Value="0,8,0,4" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
